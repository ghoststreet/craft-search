{#
    Advanced tab body — admin/ops focused.
#}
{% include 'ai-search/_dashboard/_kpi-ops' with {
    aggregates: aggregates,
    seriesCost: seriesCost,
    seriesDuration: seriesDuration,
    budget: budget,
    budgetClass: budgetClass,
    budgetPct: budgetPct,
    stats: stats,
    coveragePct: coveragePct,
    daysWithData: daysWithData,
    minNRate: minNRate,
    minNDelta: minNDelta,
    minDaysBudgetEta: minDaysBudgetEta,
} only %}

<div class="dashboard__cols">
    <div class="pane">
        <h3 class="dash-title">Coverage by site</h3>
        {% if coverage|length and coverageTotal > 0 %}
            <div class="dash-chart">
                <canvas data-craftsearch-chart="stacked-bar" data-craftsearch-series="{{ coverage|json_encode|e('html_attr') }}"></canvas>
            </div>
        {% else %}
            <p class="dash-empty">No entries indexed yet — <a href="{{ cpUrl('ai-search/index') }}">run your first sync</a>.</p>
        {% endif %}
    </div>

    <div class="pane">
        <h3 class="dash-title">Recent errors</h3>
        <table class="data fullwidth">
            <thead>
                <tr>
                    <th>Query / message</th>
                    <th class="rightalign">When</th>
                </tr>
            </thead>
            <tbody>
                {% for e in recentErrors %}
                    <tr>
                        <td><a href="{{ cpUrl('ai-search/insights/' ~ e.id) }}">{{ (e.query ?: e.errorMessage ?: 'error')|slice(0, 60) }}{% if (e.query ?: e.errorMessage ?: '')|length > 60 %}…{% endif %}</a></td>
                        <td class="rightalign"><span class="light">{{ e.dateCreated|date('M j, g:ia') }}</span></td>
                    </tr>
                {% else %}
                    <tr><td colspan="2" class="centeralign light">No errors recorded — search is healthy.</td></tr>
                {% endfor %}
            </tbody>
        </table>
        {% if recentErrors|length %}
            <p class="dash-cta"><a class="go" href="{{ cpUrl('ai-search/insights?tab=history&errorsOnly=1') }}">View all errors</a></p>
        {% endif %}
    </div>
</div>

{% include 'ai-search/_dashboard/_slow-queries' with { slowQueries: slowQueries } only %}

{% include 'ai-search/_dashboard/_recommendations' with { recommendations: recommendations, scope: 'advanced' } only %}
